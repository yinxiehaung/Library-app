
  # OPAC

  This is a code bundle for OPAC. The original project is available at https://www.figma.com/design/jKpFTuHTKwDlSds70d2AM8/OPAC.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  